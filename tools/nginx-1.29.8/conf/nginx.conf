# Auto-generated by python -m wsd.lab_setup
worker_processes  1;
error_log   C:/Users/Adam/NetworkSecurityProject/web_scraping_detector_project/tools/nginx-1.29.8/logs/error.log warn;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    log_format combined_custom '$remote_addr - $remote_user [$time_local] '
                               '"$request" $status $body_bytes_sent '
                               '"$http_referer" "$http_user_agent"';

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen       8039;
        server_name  localhost 127.0.0.1;

        access_log  C:/Users/Adam/NetworkSecurityProject/web_scraping_detector_project/tools/nginx-1.29.8/logs/access.log  combined_custom;
        root   C:/Users/Adam/NetworkSecurityProject/web_scraping_detector_project/website_lab;
        index  index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location = /healthz {
            default_type text/plain;
            return 200 'ok';
        }
    }
}
